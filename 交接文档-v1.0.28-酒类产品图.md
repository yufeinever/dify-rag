# v1.0.28 酒类产品图 Workflow + Tool MVP 交接文档

更新时间：2026-07-16（Asia/Shanghai）

## 1. 接手入口与强制约束

- 唯一源码与 Git 工作区：`/opt/mmb-dify/current`
- 当前分支：`codex/v1.0.28-product-image-workflow`
- Git 远端：`origin`（GitHub SSH）
- 必须从 Windows 直接 SSH 到 150 生产机操作，不要经 118 中转；Windows 本地 `D:\\dify-rag` 仅作为 SSH/命令入口。
- 实现、测试、构建、版本记录、提交、推送和部署均在 150 生产机完成。
- 工作区已有大量本任务改动及无关未跟踪文件。必须精确暂存，禁止提交 `.env`、备份、输出图片、密钥和无关文件。
- 不得读取、打印、写入交接文档或提交任何 API Key、Bearer Token、数据库密码。
- 生产数据库存在已知 TOAST 损坏，禁止未经确认删除或修复损坏行。

## 2. 用户最终目标

在现有 Dify 上实现“酒类产品图 Workflow + Tool MVP”，不新增 Dify 工作台页面：

1. 风格定稿：参考图和代表 SKU 生成 3 个候选，设计师选定系列母版。
2. 批量生成：Excel 驱动 20-100 个 SKU，支持同一主体映射多个渠道竖版。
3. 修订与导出：查询批次、指定 SKU 重生、PS 修订 PNG 回传、批准版本、ZIP 导出。
4. 固定输出：`750x750 RGBA PNG` 和 `750x1280 sRGB JPG`。
5. 模型只生成视觉母图；产品名、酒精度、容量等正式文字由 Pillow 模板绘制。
6. Skill 仅作为后续可选交互封装，不处理图片、队列和存储；第一版以 Workflow + Tool 跑通为准。

## 3. 当前生产状态（已上线）

复核时间：2026-07-16。

### Poster Service

- 容器：`poster-service-poster-service-1`
- 当前镜像标签：`mmbai/poster-service:local`
- 当前镜像摘要：`sha256:ff5b42a6d953e558d71264e4b9f5f5e248f21cc23a1f7a889a5bbbe15297b043`
- 端口：宿主机 `8088` -> 容器 `8088`
- 健康检查：`GET http://127.0.0.1:8088/health` 返回 `status=ok`
- 模型：`gpt-5.5`，图片模式 `responses`
- OpenAI 兼容接口：`http://186.241.120.199:10088/v1`
- 当前批处理并发：`2`
- 抠图模型：`isnet-general-use`
- `/models` 已做鉴权验证：HTTP 200，约 0.341 秒，模型列表包含 `gpt-5.5`。
- 用户此前要求接口地址之外“其他不变”，因此生产运行镜像特意保持旧镜像，尚未部署并发修复镜像。

### Dify API

- 生产镜像：`mmbai/dify-api:dify-mmb-v1.0.28`
- 镜像摘要：`sha256:20c003f5754a6f36b80ae40172b3c3d178fb9e668b67d12cd75ed74fe829f9f6`
- API、Worker、Worker Beat、WebSocket 容器均在运行；API 健康状态正常。

## 4. 待发布内容（源码/镜像已准备，未上线）

- Poster 待发布镜像：`mmbai/poster-service:pending-v1.0.28-concurrency-fix`
- 摘要：`sha256:680c17e48d8f22878c149ca75c04e029678f905a24eea04867c99ff34b9a0b33`
- 该镜像包含：批次默认并发从 2 降为 1；失败原因为空时提供兜底信息。
- 是否部署该镜像需先向用户确认，因为用户最近明确要求仅替换出图接口、其他保持不变。

## 5. 已完成实现

- Excel 批量解析，单批最多 100 SKU。
- 36 个方图主体到 37 个竖版的一对多映射测试。
- 风格候选、批次创建、状态查询、单项重生、PS PNG 回传、审批、ZIP 导出。
- 模型固定生成 `1024x1024` 母图。
- 使用 `rembg/isnet-general-use` 生成透明主体。
- Pillow 模板输出 `750x750 RGBA PNG` 与 `750x1280 RGB JPG`，正式文字不交给模型生成。
- 批次 JSON 持久化、服务重启恢复、不可变修订、失败原因、审批记录和 manifest。
- Dify 资产自动登记、预览和受信任 URL 校验。
- Nginx Poster 输出挂载修复。
- 302 个历史 Poster 文件（约 255 MB）已无覆盖迁入新目录。

## 6. 测试状态

- Poster 测试：`25 passed`
- Dify API 资产发现测试：`11 passed`
- Poster/API Ruff：通过
- Dify Workflow 无费用状态链路：通过
- 尚未完成：5 SKU 全成功人工验收、完整 36/37 系列运行、最终 ZIP 审批导出。

## 7. Dify Workflow v0.0.4

应用 ID 保持不变：

| 工作流 | App ID | Workflow ID | 权限 |
| --- | --- | --- | --- |
| 风格定稿 | `aec987cc-991d-493a-be71-7902c9e99697` | `9a74906d-e31e-4248-b5cf-3b0daca97ce8` | 设计师可用 |
| 批量生成 | `27dd329b-2208-4d53-933d-4cd8251ec131` | `76aacac9-ac79-4d57-aac1-16dae80a15b6` | 设计师可用 |
| 审核导出 | `4d6c66af-07da-4850-a246-5caa510118f9` | `918e0df4-59cc-4630-a85b-0f1e3919f93f` | 仅管理员/所有者 |

## 8. 真实 5 SKU 试跑

批次 ID：`f17584b7-131c-4d80-8503-afe16077b0bd`

| SKU | 结果 |
| --- | --- |
| IPA | 成功，`needs_review` |
| 黑啤 | 成功，`needs_review` |
| 海盐荔枝 | 超时失败 |
| 山楂草莓 | 超时失败 |
| 龙井小麦 | 超时失败 |

当前批次未审批、未导出 ZIP，也未运行完整系列。186 接口已切换且速度明显改善；若用户批准部署待发布镜像，可用串行并发 1 重生 3 个失败 SKU。

## 9. 风险与安全事项

### 临时 App API Key

2026-07-16 只做了文件存在性检查，未读取内容：

- `/tmp/v1028-batch-app-token`：不存在
- `/tmp/v1028-review-app-token`：不存在
- `/tmp/v1028_temp_app_token.py`：仍存在

接手后第一优先级：检查数据库/API 中临时 App Key 是否仍有效并撤销，然后删除临时管理脚本。全程不得输出 Token 内容。

### 数据库

`workflow_node_executions` 存在 PostgreSQL TOAST 损坏：

`missing chunk number 0 for toast value 41594 in pg_toast_17239`

不要擅自删除或修复损坏行。后续数据库备份继续排除该表数据。

### 备份

备份目录：`/opt/mmb-dify/backups/v1.0.28-product-image-workflows-20260715`

其中 SQL 文件权限已设为 `600 root:root`。

## 10. Git 与版本状态

- 本任务实现尚未整体提交、打 Tag 或推送。
- `VERSION` 当前仍是 `v1.0.26`。
- `DEPLOYMENT_VERSION` 当前仍记录 `v1.0.27`，且部分镜像字段不是当前生产实况。
- `版本迭代说明.md` 尚未加入 `v1.0.28`。
- 发版必须按顺序完成 `v1.0.28`，不得跨版本号。
- 当前工作区存在多项已修改文件及大量无关未跟踪备份；只能精确暂存本任务文件。

## 11. 建议接手顺序

1. 进入 `/opt/mmb-dify/current`，阅读根目录和相关子目录 `AGENTS.md`，核对 `git status`。
2. 撤销临时 App API Key 并删除 `/tmp/v1028_temp_app_token.py`，不输出任何秘密。
3. 向用户确认是否允许部署 `pending-v1.0.28-concurrency-fix`；未经确认保持生产现状。
4. 若获准，部署待发布镜像，确认健康信息并发为 1、接口仍为 186，然后串行重生 3 个失败 SKU。
5. 请设计师人工检查 5 个代表 SKU；批准后再运行完整 36/37 系列。
6. 自动校验尺寸、格式、Alpha 通道、sRGB、正式文字字段、命名和 ZIP manifest。
7. 更新 `VERSION`、`DEPLOYMENT_VERSION` 和 `版本迭代说明.md` 为 `v1.0.28`，时间使用实际发布时间。
8. 重新执行 Poster/API 定向测试和 Ruff，记录结果。
9. 精确暂存本任务文件，复核 staged diff 后提交、推送、部署并打顺序 Tag。
10. 发布后记录实际镜像摘要、提交 SHA、部署时间和回滚方法。

## 12. 常用只读验证命令

```bash
cd /opt/mmb-dify/current
git branch --show-current
git status --short
docker ps --format '{{.Names}}|{{.Image}}|{{.Status}}' | grep -E 'poster|mmb-dify-v010-api'
docker inspect poster-service-poster-service-1 --format '{{.Image}}'
curl -fsS http://127.0.0.1:8088/health
docker image inspect mmbai/poster-service:pending-v1.0.28-concurrency-fix --format '{{.Id}}'
```

不要用会打印整个容器环境变量的命令，以免泄露 API Key。
