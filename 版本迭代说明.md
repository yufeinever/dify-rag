# Dify-MMB 版本迭代说明

## v1.0.23 - 2026-07-11

- 类型：视频角色与场景参考图一致性修复。
- 目标：参考图以真实 `input_image` 进入图片生成模型，角色身份和场景结构不再退化为 URL 文字提示。
- 验证源码提交：`67eacde5`。
- Dify 应用：`MMB 视频创作工作流`（`575c4fa3-4268-4ff5-8100-248e4a3739c7`）。
- 工作流运营版本：`v0.0.10`。
- 发布工作流 ID：`7e637e9b-5ec1-41bb-8666-97260c1b61c6`。
- Poster 镜像：`mmbai/poster-service:local`（`sha256:43e57b795a9aa9d2ce45150f6ee76cdde7e5668700d55de23ba9c909a57d2a47`）。
- 数据库迁移：无 schema migration；更新并发布视频工作流图。
- 重启范围：仅 `poster-service-poster-service-1`；Dify 主栈、Postgres、Redis 和 Weaviate 均未重启。

### 变更内容

- 视频输入拆分为角色参考图（最多 3 张）、场景参考图（最多 3 张）和其他参考图（最多 5 张）。
- poster-service 下载显式视觉参考，校验 HTTP 状态、内容类型、真实图片格式、大小和超时，再按角色、场景、其他顺序转换为 Responses API `input_image`。
- 场景首帧强制包含已生成角色图；角色图为空、上传 URL 无效或图片校验失败时直接中止，不再静默使用纯文字生图。
- 用户角色图存在时禁用默认熊；未提供角色且规划器明确启用默认角色时使用内置 MMB 熊图。
- 旧海报工作流的默认熊素材映射到内置图片；没有新 `source` 标记的旧知识素材仍保持文字提示兼容。
- 返回 `reference_image_stats`，记录请求、加载、角色、场景、其他、默认熊和生成角色图片数量。

### 验证记录

- poster-service 12 项测试通过，覆盖图片顺序、默认熊互斥、显式开关、旧素材兼容、格式错误、空 URL、数量上限和生成角色额外锚点。
- 工作流载荷 5 种场景通过：无图、仅角色图、仅场景图、角色/场景/其他同时上传、角色定稿图缺失时阻断。
- 生产容器集成检查确认 4 张图片按 `character, character, scene, other` 顺序进入真实图像输入，统计为 requested=4、loaded=4。
- 使用生产目录 Compose 构建并重建 poster-service，Docker 依赖层全部复用缓存；健康检查返回 `status=ok`、`image_mode=responses`。
- 未触发产生供应商费用的新图片或视频任务；图片 API 请求结构和生产容器下载/校验链路已验证。

### 回退方式

- 将 poster-service 运行目录恢复为 `ops-backups/v1.0.23-video-reference-20260711/poster-runtime` 中的备份并重建该服务。
- 从数据库表 `mmb_backup_v1023_video_reference_workflows_20260711` 恢复该应用工作流，再重新发布 `v0.0.9`。
- 本次不需要回退数据库结构、Dify 主栈镜像或数据卷。

## v1.0.22 - 2026-07-11

- 类型：生成视频本地持久化紧急修复。
- 目标：生成成功后优先保存到 Dify 自有存储，由本地文件提供播放和下载，不再把 xAI 临时 CDN 当作长期资产源。
- 生产源码：`/opt/mmb-dify/current`
- 来源版本：`v1.0.21` / `3d33a9089850c5e2a76d92b63dab9e931875b110`
- 验证源码提交：`4d454b12ebafb1d72c4e0baf4087917e7ba7d144`。
- 部署时间：`2026-07-11 23:12:57 +08:00`。
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.22`（`sha256:bb9329260b3e39d1590da2b01bc285638635eeb831089828fe8078448cb9a42b`）
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.22`（`sha256:9b48dd6d5b3526af925a3f997b9d50160f43a948808963b2e7b352ecf07ecd3d`）
- 数据库迁移：无 schema migration；新增 `ToolFile` 和资产关联数据。
- 重启范围：已更新 `api`、`api_websocket`、`worker`、`worker_beat`、`web`，未重启 Postgres、Redis、Weaviate。
- Nginx：未重启容器；因 API/Web 容器 IP 更新，于 `2026-07-11 23:13:27 +08:00` 执行配置 reload。

### 根因

- `v1.0.21` 按原方案只保存 xAI `vidgen.x.ai` URL，没有复制 MP4；实测 xAI 对旧对象执行生命周期清理，截图中的“暗影跃斩”原始 URL 已返回 HTTP 404。
- 排查时共 19 条视频资产：10 条源文件仍可访问，9 条已返回 404。可用与失效的时间边界约为生成后 28.5 小时，不能作为长期存储保证。
- 资产索引、工作流结果、角色图和场景图没有丢失；丢失的是上游已删除的 9 个 MP4 本体。

### 变更内容

- 成功工作流登记视频资产时，立即下载受信任的 MP4，写入 Dify 原生存储并创建 `ToolFile`；本地 SHA256、原始来源 URL 和持久化时间写入扩展元数据。
- 重复回调不会把已经持久化的视频降级回远程 URL，也不会重复创建本地副本。
- “我的作品”按工作流运行 ID 查找本地视频资产，优先返回每次请求新生成的本地签名地址；已确认丢失的旧视频显示失败原因，不再展示 `0:00` 黑屏。
- 资产管理详情对 `tool_file` 视频强制使用本地 `preview_url`，不再优先使用残留的外部来源字段。
- `/files/tools/...` 增加单区间 HTTP Range 响应，返回 `206`、`Content-Range` 和 `Accept-Ranges`，支持读取时长、播放和拖动。
- 新增幂等命令 `flask recover-generated-videos`，只导入 `<asset-id>.mp4`，只对有 `<asset-id>.unavailable` 标记的记录登记源文件失效，避免误伤新生成视频。

### 紧急恢复

- 在 xAI 清理前抢救 10 个仍可访问的 MP4，保存于 `/opt/mmb-dify/backups/v1.0.22-video-rescue-20260711220331`，并逐个确认 ISO MP4 文件头和 SHA256。
- 导入前备份 `generated_files` 与 `tool_files` 到 `/opt/mmb-dify/backups/v1.0.22-video-localization-pre-import-20260711223848`。
- `generated_files.sql` SHA256：`f9f3b9eee6ec62fa3b20de1bef266be7bb00ef8dba5ee01028b1a135bdc76bc6`。
- `tool_files.sql` SHA256：`777af432b9995715b4602f0d190c9658558f9e57efed1da96f83de96326ee3cf`。
- 正式导入结果：`scanned=19, persisted=10, skipped=9, errors=0`；本地视频总大小 `45,392,023` 字节。
- 二次 dry-run：`persisted=0, unavailable=9, errors=0`，确认导入幂等；9 条 404 记录保留索引并标记为源文件失效。

### 验证记录

- 后端生成资产、历史权限、文件 Range、工作流仓储相关测试 49 项通过；Ruff check 与 format check 通过。
- 前端资产预览测试 6 项通过，覆盖本地 `ToolFile` 地址优先级。
- 使用生产 Compose 完成 API/Web Docker 构建，依赖层复用缓存；`pnpm build` 与 `pnpm build:vinext` 均成功。
- 五个目标服务均运行 `v1.0.22` 镜像，API health 为 `healthy`；公网首页返回 `307` 正常跳转，实际 Console API 请求持续返回成功响应。
- 对本地视频 `c53d3dc6-913b-442e-967c-9ac728d8542d` 的公网签名 URL 发起 `Range: bytes=0-1023`，返回 `206 Partial Content`、`Content-Type: video/mp4`、`Accept-Ranges: bytes`、`Content-Range: bytes 0-1023/2991892`。
- Range 响应体为 1024 字节，文件头 `00 00 00 20 66 74 79 70 69 73 6f 6d`，确认由 Dify 本地存储提供有效 ISO MP4，可供浏览器读取时长和拖动播放。
- 未擅自触发会产生供应商费用的新视频任务；新工作流成功回调的自动本地化由工作流集成测试覆盖，生产持久化函数已通过 10 条真实抢救视频导入验证。

### 回退方式

- API/Web 镜像恢复为 `mmbai/dify-api:dify-mmb-v1.0.21` 和 `mmbai/dify-web:dify-mmb-v1.0.21`。
- 使用 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`。
- 已保存的本地视频和 `ToolFile` 记录保留，不删除或回退；即使应用代码回退，抢救文件仍不会丢失。

## v1.0.21 - 2026-07-11

- 类型：统一生成内容资产库。
- 目标：将原“生成文件库”升级为“资产管理”，统一查看图片、海报、文档、PPT、表格、音频、视频和压缩包。
- 生产源码：`/opt/mmb-dify/current`
- 来源版本：`v1.0.20` / `e75482d1`
- 验证源码提交：`484027830a88376849e489e5a489c16abba2caa4`
- 发布标签：`v1.0.21`
- 部署时间：`2026-07-11 21:08:34 +08:00`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.21`（`sha256:9c290b1944e33eb7595984c0a74b1693f3cc2d68caa0eabf083d3c1c5b07f680`）
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.21`（`sha256:561cc9d1ca505f5019fa4098bb598f6dfb05469a92100386a2e8d996c59732f0`）
- 数据库迁移：`b2c3d4e5f6a8`，扩展 `generated_files` 为本地文件与可信远程 URL 的统一索引。
- 迁移前备份：`/opt/mmb-dify/backups/v1.0.21-generated-assets-pre-migration-20260711205620`；schema SHA256 为 `c52f718f1e418fb284023ae7e68a444e066a66aed655c69f7ffeaae97647f961`，data SHA256 为 `ac07dabe170af57a2c9ac2241649b4349f1ecc665d55ca412e4dcf2c62c2878b`。
- 重启范围：`api`、`api_websocket`、`worker`、`worker_beat`、`web`；不重启 Postgres、Redis、Weaviate，不操作持久卷。
- Nginx：未重启容器；因 Web 容器 IP 更新于 `2026-07-11 21:10:28 +08:00` 执行配置 reload，随后公网恢复 200。

### 变更内容

- 保留 `/document-management` 地址，顶部入口改为“资产管理”，默认显示“生成内容”；具备知识库权限的成员可切换“知识库文档”。
- 管理员和所有者可查看全租户、全渠道资产；普通后台成员只能查看自己的资产，入口不再依赖知识库权限。
- 支持类型、使用者、应用、来源和日期筛选、分页及搜索防抖；移动端也可筛选使用者和应用。
- 本地 `ToolFile` 自动登记图片、Office、音频、压缩包及工具输出；成功视频和海报工作流只从受信任结构化输出字段登记远程资产。
- 纯 Workflow 最终输出中的 Dify 原生 `ToolFile` 也会登记，并严格校验文件租户和创建者；不会递归抓取任意用户输入链接或伪造文件 ID。
- 视频以成品 MP4 为单一主资产，场景图作为列表缩略图，角色图和场景图作为详情附件；列表不请求 MP4，详情使用 `preload="metadata"` 直连 xAI CDN。
- 海报以高清 PNG 为主资产、缩略图为列表预览；远程文件只保存索引和 URL，不复制视频或海报文件。
- 远程来源键按租户、应用、使用者和规范化 URL 幂等，忽略签名查询参数；重复回调可刷新过期签名 URL，不会跨用户合并。
- 新增通用 Service API：列表、详情、下载和 Office 转 PDF 预览；使用应用 API Key 加稳定 `user`，严格限制租户、应用和 `EndUser`。
- 命名空间用户按 `<channel>:<id>` 展示渠道和稳定短标识；历史无命名空间身份显示为“Service API 用户”。
- 外链失效时保留资产记录并显示“源文件暂不可用”；删除操作只软删除索引，不删除聊天记录或源文件。

### 历史补录

- 迁移前已有 17 条文件索引全部保留，并按唯一 assistant `MessageFile` 关系修复其中 11 条历史所有者归属。
- 首次 dry-run 实际为 `scanned=98, created=80, skipped=18, errors=0`；正式执行补录 `created=80, errors=0`。
- 二次 dry-run 实际为 `created=0, skipped=98, errors=0`，确认重复执行幂等。
- 补录 28 条遗漏的 assistant `ToolFile`、17 条可用视频和 35 条去重海报；当前共 97 条主资产，其中 `message_file=45`、`workflow_video=17`、`workflow_poster=35`。
- 生产有 18 条外层成功且包含视频字段的工作流记录，其中 1 条业务状态为 `failed` 且没有视频 URL，正确排除，可用视频验收数为 17。
- `smoke.pptx` 无法证明为正式模型产物，明确不补录；后续模型 PPTX 由上传接口在创建时自动登记。
- 重复来源键为 0，17 条视频资产均无本地视频副本；已确认的失败视频工作流和 `smoke.pptx` 对应可用资产均为 0。

### 验证记录

- 后端生成资产、同步/异步工作流仓储、PPT 产物、聊天/工具登记相关测试共 113 项通过；Ruff 检查和格式检查通过。
- 前端资产列表、权限入口、视频/图片/Office 预览和分页测试 28 项通过；目标文件 `vp lint` 无错误。
- 生产等价 Web Docker builder 构建通过，`pnpm build` 与 `pnpm build:vinext` 均成功。
- 全量前端类型检查仍有本次变更前已有的测试类型错误；本次资产管理文件无新增类型错误，生产构建成功。
- Alembic 当前 head 为 `b2c3d4e5f6a8`；迁移前 17 条数据备份及 SHA256 已复核，迁移与 80 条补录执行无错误。
- 管理员公网列表显示 97 条、图片 35、视频 17、文档/PPT/表格 45，共 2 页；类型、使用者、应用、来源和日期筛选均加载正常。
- Console 真实 HTTP 验收中，普通成员即使传 `include_all=true` 或指定管理员 owner，列表仍只有自身范围；访问他人详情、下载、转换和删除均为 404，目标未被删除。当前普通成员自身资产数为 0，因此正向自有资产读取由自动化测试覆盖。
- Service API 正确 App Key 与正确 `user` 可列出 3 条并读取详情；同应用跨用户、同用户值跨应用和随机 UUID 的详情、下载、转换全部为 404。
- 视频列表捕获 64 个资源请求，MP4 请求为 0；视频详情使用 `preload="metadata"`、不自动播放，直接访问 `vidgen.x.ai`，场景图和角色图正常显示。
- 图片详情成功加载 1080x1440 海报原图；Office 详情返回 `200 application/pdf`、`Content-Length: 746146` 并生成 Blob iframe；移动端 390x844 下统计、五类筛选和资产列表无页面横向溢出，验收后已恢复默认视口。
- 五个目标服务运行新镜像且 restart count 为 0；Postgres、Redis、Weaviate 未重启。Nginx reload 后未再发现资产功能相关错误或 5xx，API health 返回 `status=ok`。
- 两个构建测试容器 `mmb-v1021-api-test`、`mmb-v1021-web-test` 已在发布收尾删除，保留构建缓存与镜像。

### 回退方式

- 将 API 镜像恢复为 `mmbai/dify-api:dify-mmb-v1.0.20`，Web 镜像恢复为实际生产基线 `mmbai/dify-web:dify-mmb-v1.0.18`。
- 使用 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`。
- 新增字段和资产索引保留，不执行破坏性数据库降级；远程视频和海报始终保留在原 CDN，不存在需要清理的本地副本。

## v1.0.20 - 2026-07-11

- 类型：发布海报异步预览加载优化。
- 目标：避免聊天结果首次展示时直接加载 2MB 左右 PNG 原图，降低 CDN 冷回源等待时间。
- Dify 应用：`发布海报助手-后台生成`（`13f7a06c-769d-474c-8863-613e9aeb25cb`）
- poster-service：`0.1.2`，保留高清 PNG 原图输出。
- 数据库变更：更新异步应用 draft 与已发布 workflow 的完成态回答节点，无 schema migration。
- 镜像重建：`poster-service`。
- 重启服务：仅 `poster-service`。

### 变更内容

- 异步任务完成后，聊天 Markdown 使用 `thumbnail_url` 展示预览图，原始 `poster_url` 仅作为“查看/下载高清原图”链接。
- 新生成缩略图从最长边 1280、JPEG quality 86 调整为最大 540x720、quality 80，并继续使用渐进式 JPEG。
- 保持原始 1080x1440 PNG、任务查询接口、任务号和生成流程不变；旧正式 `发布海报助手` workflow 不变。

### 验证记录

- 样本 PNG 为 2,044,218 bytes；源站直读约 0.006 秒，CDN 命中后公网下载约 1.24 秒。
- 原缩略图为 218,049 bytes，CDN 冷回源首访约 6.3 秒、命中后约 0.28 秒，确认主要瓶颈为唯一新 URL 的首次 CDN 回源。
- 验证异步应用完成态节点原先误用 `poster_url` 作为 Markdown 图片，已改为 `thumbnail_url`。
- 真实样本按新参数生成的 540x720 渐进式 JPEG 为 70,275 bytes，约为 2.04MB 原始 PNG 的 3.4%。
- 通过 Dify Chat API 查询既有任务，约 1 秒返回，Markdown 图片已使用 `-thumb.jpg`，高清链接仍指向原始 PNG。

### 回退方式

- 从 `ops-backups/v1.0.20-poster-preview-20260711` 恢复 poster-service 的 `main.py.before`。
- 恢复备份的异步 workflow graph，并重新构建、启动 `poster-service`。

## v1.0.19 - 2026-07-11

- 类型：MMB 视频创作工作流知识库接入。
- 目标：在视频方案设计前检索现有 MMB 品牌、产品和业务资料，减少品牌事实遗漏与虚构。
- Dify 应用：`MMB 视频创作工作流`（`575c4fa3-4268-4ff5-8100-248e4a3739c7`）
- 工作流运营版本：`v0.0.9`
- 发布工作流 ID：`4db7441c-0f7e-40ec-9d66-9914ab09a818`
- 知识库：`MMB统一材料知识库-v4.1`（`29f282db-a0c7-4f9b-a360-fdb921d53948`）
- Web/API 镜像：沿用 `v1.0.18`，无镜像重建
- 数据库变更：仅发布工作流数据，无 schema migration
- 重启服务：无

### 变更内容

- 在主链 `start → design_llm` 中插入“检索 MMB 品牌资料”知识检索节点，主链调整为 `start → brand_knowledge → design_llm`。
- 使用用户的视频需求检索唯一的统一材料知识库，采用多路检索配置、Top K 10，并把检索结果作为 GPT-5.5 方案设计节点的 context。
- 提示词要求只使用与本次视频相关的品牌事实和业务约束，不生硬照抄资料，不虚构产品参数、承诺或公司事实。
- 用户上传的参考图继续作为角色、产品、服装、场景和视觉风格的最高优先级依据。
- 发布图包含 21 个节点、19 条连线；未修改 API、数据库字段、镜像或 Compose 服务。

### 验证记录

- 通过 Dify 原生草稿接口回读图结构，确认知识节点、数据集选择器、主链连线和 `design_llm.context` 已持久化。
- 单独运行知识检索节点，查询“MMB 自助鲜啤设备和品牌定位”返回 HTTP 200，命中 `MMB统一材料知识库-v4.1`。
- 首条结果为瞢瞢熊智慧鲜啤室外展位及自助出酒设备资料，相关度约 `0.879`。
- 发布后通过工作流版本历史确认 `v0.0.9` 指向发布工作流 ID，图结构仍为 21 个节点、19 条连线。
- 公网单节点调试因上游 30 秒网关限制返回 504；绕过公网代理直连 Dify API 后约 39 秒成功返回，确认是调试入口超时而非检索失败。

### 回退方式

- 在 Dify 工作流版本历史中重新发布上一版本 `v0.0.8`，或使用 `/opt/mmb-dify/current/ops-backups/v1.0.19-video-rag/draft-before-rag.json` 恢复发布前草稿后重新发布。
- 本次没有镜像、服务配置和数据库结构变更，不需要重启容器或执行迁移回退。

## v1.0.18 - 2026-07-11

- 类型：探索入口与应用置顶生产修复。
- 目标：普通用户进入探索时直接打开首个 Web App，降低误入应用库概率，并恢复侧栏置顶功能。
- 验证源码提交：`ddf9260803bbbb964f1b89a179285e084b719930`
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.18`（`sha256:fe0b5c654533...`）
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.18`（`sha256:d3496e54dde7...`）
- 数据库变更：无
- 重启服务：`api`、`api_websocket`、`web`

### 变更内容

- 访问裸 `/explore/apps` 时按服务端顺序打开第一个可用 Web App；无已安装应用时继续显示应用库。
- 侧栏“应用库”固定到 Web App 列表下方、收起按钮上方，并使用 `/explore/apps?view=library` 显式停留在应用库。
- 置顶操作先乐观更新侧栏排序，请求失败时回滚并显示错误提示，成功后重新读取服务端状态。
- 确认公网代理会拦截原 PATCH 请求并返回空 400；新增 `POST /installed-apps/{id}/pin` 兼容端点，保留 PATCH 供旧客户端和直连使用。

### 验证记录

- API installed-app 控制器测试 15 项通过；Web 应用库、侧栏、置顶缓存和生命周期测试 43 项通过。
- `pnpm build` 与 `pnpm build:vinext` 均在生产 Docker 路径构建成功。
- 内置浏览器确认裸探索入口进入 `MMB智囊`，底部应用库可显式打开且不会自动跳走。
- 公网域名 POST 置顶和取消置顶均返回 200，验收后已恢复测试应用原状态。
- API 健康检查通过，API WebSocket 与 Web 容器正常运行；Postgres、worker 和 nginx 未重启。
- 全量前端类型检查仍存在本次变更前已有的测试类型错误；本次目标文件测试与 lint 无新增错误。

### 回退方式

- 恢复 `docker/.env.bak.before-explore-pin-v1018-20260710234800`，或将 Web/API 镜像分别恢复为 `v1.0.17` 与 `v1.0.14`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket web`。
- 无数据库迁移；回退后旧 PATCH 置顶仍会继续受公网代理限制。

## v1.0.17 - 2026-07-10

- 类型：MMBAI 官方帮助文档本地镜像与品牌白标发布。
- 目标：帮助入口不再依赖 `docs.dify.ai`，并将文档名称、正文、导航和 URL 路径统一为 MMBAI。
- 文档版本：上游 `release/1.14.2`，共 489 个中英日页面。
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.17`
- 文档镜像：`mmbai/mmbai-docs:1.14.2`
- 数据库变更：无

### 变更内容

- 在 150 本地托管文档源与图片资源，并通过 `/mmbai-docs/` 暴露。
- 文档中的 `Dify/dify` 品牌文字、导航与内部路径替换为 `MMBAI/mmbai`。
- 使用本地 MMBAI Logo；Dify 所有文档链接切换到本地文档入口。

### 验证记录

- 文档文本与路径扫描中 `Dify/dify` 残留为 0。
- 浏览器验收页面标题为“核心概念 - MMBAI 官方文档”，正文无 Dify，图片无破图。
- 公网文档 URL 返回 HTTP 200；Web、docs、nginx、api 容器正常。

### 回退方式

- 将 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.16`，移除 Nginx `/mmbai-docs/` 路由并停止 docs 服务。

## v1.0.16 - 2026-07-10

- 类型：MMB 视频作品列表场景缩略图优化。
- 目标：修复视频地址被当作图片导致的裂图，并避免列表加载高清原图。
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.14`（未变更）
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.16`
- 数据库变更：无

### 变更内容

- 作品列表优先展示场景首帧对应的 poster-service `-thumb.jpg` 缩略图。
- 不再将 `video_url` 交给 `<img>`，没有受支持缩略图时显示视频占位图标。
- 缩略图启用浏览器懒加载；详情页继续使用原图和视频播放器。

### 验证记录

- 作品列表测试 5 项通过；Web 生产镜像构建与滚动部署通过。
- 样本原图约 1.6–1.9 MB，对应缩略图约 140–170 KB。

## v1.0.15 - 2026-07-10

- 类型：MMB 视频工作流历史切换重复运行热修复。
- 目标：从“我的作品”返回“运行一次”时不再自动重新提交旧任务。
- 生产路径：`/opt/mmb-dify/current`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.14`（未变更）
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.15`
- 数据库变更：仅将 3 条已无执行进程的误触发记录修复为 `stopped`，无迁移

### 变更内容

- 进入“我的作品”时清空旧的 `controlSend` 信号，防止结果组件重挂载后重放请求。
- 选中历史记录和回填历史参数时同步清理旧信号，覆盖所有历史返回路径。
- 备份并停止 3 条误触发记录；它们均未进入角色图、场景图或 Grok 视频节点。

### 验证记录

- TextGeneration 回归测试 6 项通过，包含“运行一次 → 我的作品 → 运行一次”的信号清零测试。
- Web 生产镜像构建通过，Web 与 nginx 完成滚动更新。
- 生产 Web 正常运行，MMB 视频工作流运行中记录为 0。

### 回退方式

- 将 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.14` 并重建 Web 服务。

## v1.0.14 - 2026-07-10

- 类型：MMB 视频工作流“我的作品”账号历史改造。
- 目标：让内部 Explore 用户退出后仍能查看自己此前的视频作品、预览/下载视频并回填参数再次生成。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.13` / `206faea0`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.14`
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.14`
- 镜像重建：API 与 Web
- 数据库变更：仅更新 MMB 视频工作流运营版本说明为 `v0.0.8`，无 schema migration
- 重启服务：`api`、`api_websocket`、`worker`、`worker_beat`、`web`、`nginx`

### 变更内容

- 在内部「探索 → MMB 视频创作工作流」增加原生“我的作品”，按当前登录账号展示生成中、成功和失败记录。
- 新增账号隔离的 installed-app 工作流历史列表与详情接口，强制限定租户、应用、installed-app 来源、账号角色和账号 ID。
- 作品详情支持脚本、分镜、角色定稿图、场景首帧、视频预览与下载；“使用这些参数”只回填用户输入，不自动发起生成。
- 列表不返回 Data URI；旧记录缺少结构化字段时从 Markdown 降级恢复标题、创作意图和正文。
- 工作流成功但缺少可播放 `video_url` 时按失败展示，避免产生“成功但无法播放”的作品。
- MMB 视频工作流输出契约确认为 `result`、`status`、`video_url`、`character_image_url`、`scene_image_url`、`script`、`storyboard`，运营版本记录为 `v0.0.8`。
- Web 构建依赖层增加可配置的 pnpm 超时和并发参数，以适配生产网络；不改变运行时行为。

### 验证记录

- 后端服务单元测试 8 项、Explore 工作流控制器测试 11 项通过。
- 前端作品列表、作品详情、侧栏、结果面板和页面协调测试通过。
- API/Web 镜像构建通过；生产接口、账号隔离、历史作品详情和页面回归验收通过。

### 回退方式

- 将 `docker/.env` 的 `DIFY_API_IMAGE` 恢复为 `mmbai/dify-api:dify-mmb-v1.0.12`，`DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.9`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`，随后重启 `nginx`。
- 本次没有数据库迁移；如需回退运营版本说明，将应用描述恢复为发布前内容即可。

## v1.0.13 - 2026-07-10

- 类型：MMB视觉PPT助手文档上传与模型文件输入修复。
- 目标：让应用同时支持图片和标准文档上传，并将文档内容真实传入 OpenAI Responses API。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.12` / `1214f618`
- 发布引用：`v1.0.13` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.9`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.12`
- 插件：`mmb/openai_responses_full_tools_provider:0.1.1@c9a6c2a46a4eecb3c9542b7bec8fb8169172926d0c0cca53b40fe6576b02923a`
- 镜像重建：否
- 数据库变更：是；更新应用当前模型配置，并创建应用配置与插件声明备份表，无 schema migration
- 备份目录：`ops-backups/v1.0.13-document-upload-20260710-110027`
- 备份表：`mmb_backup_v1013_ppt_document_upload_app_model_configs`、`mmb_backup_v1013_fulltools_plugin_declarations`
- 重启服务：`plugin_daemon`

### 变更内容

- 将 `openai_responses_full_tools_provider` 的实际生产源码纳入 `docker/plugins` 版本管理，插件版本由 `0.1.0` 升至 `0.1.1`。
- 为 `gpt-5.5` 声明 `document` 能力，保留文字、图片和既有工具调用行为。
- 将 Dify 文档附件转换为 OpenAI Responses `input_file`：本地文件使用 base64 数据，远程文件使用 URL，并保留文件名。
- 文档缺少数据和 URL 时返回明确错误，不再静默降级为提示文本。
- MMB视觉PPT助手启用 `image + document`，本地文件与远程 URL 均可上传，数量上限保持 3。
- 文档扩展名对齐 Dify 标准集合：TXT、Markdown、PDF、HTML、Excel、Word、CSV、EML、MSG、PowerPoint、XML、EPUB。
- 提示词增加附件优先规则：先提炼上传材料的主题、事实、结构和重点，再生成最多 12 页的视觉 PPT。
- 精确失效租户级插件模型列表与该模型 schema 缓存，使工作室显示“文档”开关；未清空 Redis 其它数据。

### 验证记录

- 插件 Python 编译检查通过；单元测试 17 项全部通过。
- DOCX、PDF、TXT、XLSX、PPTX 和 PNG 均上传成功，模型从两组附件中正确读取验收代号 `Blue-Harbor-2719`。
- MP3 与 MP4 在聊天请求阶段均返回 HTTP 400，确认音频、视频未被放开。
- 真实 DOCX 到 PPT 端到端验收通过：生成 2 页 `文档上传验收.pptx`，收到 `message_file` 附件事件。
- 后端模型清单中的预定义模型和自定义模型均包含 `document`。
- 工作室最终确认“视觉”和“文档”均开启，发布菜单显示“已发布”。
- API、worker 与 plugin_daemon 服务状态正常；原模型供应商凭证完整保留。

### 回退方式

- 使用备份目录中的 `openai_responses_full_tools_provider-0.1.0.rollback.difypkg` 通过同一原生升级接口恢复插件。
- 从 `mmb_backup_v1013_ppt_document_upload_app_model_configs` 或 `app_model_configs_backup.sql` 恢复应用模型配置。
- 失效 `plugin_model_providers:tenant_id:60630e96-a41c-481d-92e2-c6f83c9e4749` 和对应模型 schema 缓存，然后重启 `plugin_daemon`。
- 旧插件声明与安装包均已保留，回退不需要重建 Web 或 API 镜像。


## v1.0.12 - 2026-07-10

- 类型：Agent 普通文件附件卡片恢复修复。
- 目标：恢复 Word、Excel、PPT 等工具生成文件的附件卡片与下载入口。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.11` / `8ec285c0`
- 发布引用：`v1.0.12` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.9`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.12`
- 镜像重建：仅 API
- 数据库变更：是；仅回填 4 条仍有文件实体但缺少 `message_files` 的非图片附件记录
- 数据备份：`mmb_backup_v1012_file_card_repair_20260710`
- 重启服务：`api`、`worker`、`worker_beat`、`api_websocket`、`nginx`

### 变更内容

- 修复工具文件从 Blob 转换为 `BINARY_LINK` 后未被 Agent 附件提取器识别的问题。
- 保持图片、Blob、普通 Link 的原有行为，并将普通文件重新写入 `message_files`。

### 验证记录

- 修复前新增回归测试失败，确认 `BINARY_LINK` 被遗漏。
- 修复后 `test_tool_engine.py` 与 `test_message_transformer.py` 共 10 项通过。
- 新镜像协议烟测通过：`BINARY_LINK attachment extraction: PASS`。
- 已回填 4 条历史非图片附件，Word、Excel、PPT 对应 `message_files` 与 `message_files` 表记录均恢复。

### 回退方式

- 将 `docker/.env` 的 `DIFY_API_IMAGE` 恢复为 `mmbai/dify-api:dify-mmb-v1.0.7`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat`，随后重启 `nginx`；如需回滚历史数据，按备份表删除新增 `message_files` 并恢复原 `message_files` 字段。


## v1.0.11 - 2026-07-09

- 类型：SSRF Proxy 长耗时图片生成超时修复。
- 目标：将 Dify HTTP Request 节点经 SSRF proxy 访问 `poster-service` 的 120 秒代理超时提高到 300 秒，避免前台同步海报生成在 2 分钟左右返回 `504 Gateway Timeout`。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.10` / `db85c6e8`
- 发布引用：`v1.0.11` tag target
- 镜像重建：否
- 数据库变更：否
- 配置变更：`docker/ssrf_proxy/squid.conf.template`
- 重启服务：`ssrf_proxy`

### 变更内容

- 将 Squid `request_timeout` 从 `2 minutes` 调整为 `5 minutes`。
- 将 Squid `read_timeout` 从 `2 minutes` 调整为 `5 minutes`。
- 保持 `connect_timeout 30 seconds`、`client_lifetime 5 minutes` 和连接池相关配置不变。

### 验证记录

- 容器内实际配置确认：`request_timeout 5 minutes`、`read_timeout 5 minutes`。
- 服务状态确认：`ssrf_proxy` 重启后正常运行，`api` healthy，`worker` 正常。
- 前台 `发布海报助手` 真实海报请求验证通过：Workflow 返回 `message/message_end`，答案中包含“生成图片”和生成海报 Markdown 图片链接，不再出现 120 秒左右的 `504 Gateway Timeout`。

### 回退方式

- 将 `docker/ssrf_proxy/squid.conf.template` 中 `request_timeout` 与 `read_timeout` 恢复为 `2 minutes`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart ssrf_proxy`。


## v1.0.10 - 2026-07-09

- 类型：MMB Responses 私有插件模型列表精简与全量 LLM 迁移。
- 目标：将所有使用 LLM 的应用、Workflow 节点和租户默认 LLM 统一迁移到 `MMB OpenAI Responses Full Tools / gpt-5.5`，同时精简两个 MMB Responses 私有插件的常用 LLM 定义。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.9` / `d828ac9e`
- 发布引用：`v1.0.10` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.9`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.7`
- 镜像重建：否
- 数据库变更：是；已备份并更新 `app_model_configs`、`workflows`、`tenant_default_models`
- 运行态变更：精简 `plugin_daemon` cwd 中两个 MMB Responses 插件的 `models/llm/*.yaml`；所有 LLM 应用配置切到 Full Tools provider
- 备份目录：`ops-backups/responses-model-prune-v1010-20260709-210507`
- 备份表：`mmb_backup_llm_fulltools_v1010_app_model_configs`、`mmb_backup_llm_fulltools_v1010_workflows`、`mmb_backup_llm_fulltools_v1010_tenant_default_models`
- 重启服务：`api`、`worker`、`plugin_daemon`

### 变更内容

- `MMB OpenAI Responses Full Tools` 的 LLM YAML 只保留 `gpt-5.5.yaml` 与 `_position.yaml`。
- `MMB OpenAI Responses` 的 LLM YAML 只保留 `gpt-5.5.yaml` 与 `_position.yaml`。
- 两个 `_position.yaml` 均改为只列 `gpt-5.5`。
- 保留 speech2text、tts、text_embedding、moderation 等非 LLM 模型定义，避免影响语音和其它非聊天能力。
- `app_model_configs` 22 条 LLM 配置全部切换为 `mmb/openai_responses_full_tools_provider/openai_responses_full_tools_provider / gpt-5.5`。
- `workflows.graph` 中旧 provider 引用统一替换为 Full Tools provider，覆盖 OpenAI-compatible、基础 Responses provider 和官方 OpenAI provider 的历史引用。
- 租户默认 LLM 切换为 Full Tools `gpt-5.5`；rerank、speech2text、text-embedding、tts 默认模型保持不变。

### 验证记录

- Full Tools 插件：`python -m py_compile models/llm/llm.py models/llm/responses_tool_parser.py`
- Full Tools 插件：`python -m unittest discover -s tests -v`，11 项通过。
- Responses 插件：`python -m py_compile models/llm/llm.py models/llm/responses_tool_parser.py`
- 数据库确认：两个 MMB Responses provider 的 `provider_models` / `provider_model_credentials` 均只有 `gpt-5.5 / llm`。
- 数据库确认：`app_model_configs` 22 条全部为 Full Tools `gpt-5.5`；`tenant_default_models.llm` 为 Full Tools `gpt-5.5`；`workflows` 中旧 provider 引用为 0，Full Tools 引用 19 条。
- 服务验证：`docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart api worker plugin_daemon` 后，`api` healthy，`worker` 与 `plugin_daemon` 正常运行。
- API 烟测：使用 `MMB超级内脑` 应用 API 发送 streaming 连通性请求，返回 `OK` 并生成正常 token usage。
- 备注：运行态插件 YAML 与数据库已精简到 `gpt-5.5`；若模型供应商 UI 仍显示历史数量，属于 Dify 已安装插件元数据/缓存展示问题，本次不继续阻塞全量 LLM 迁移。

### 回退方式

- 从备份目录恢复 `full_tools_llm` 与 `base_responses_llm` 到对应插件目录的 `models/llm`。
- 如数据库曾被后续清理，可从 `provider_model_records.sql` 恢复 provider model 记录。
- 如需回滚 LLM 迁移，可从 `mmb_backup_llm_fulltools_v1010_app_model_configs`、`mmb_backup_llm_fulltools_v1010_workflows`、`mmb_backup_llm_fulltools_v1010_tenant_default_models` 恢复对应表数据。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart api worker plugin_daemon`。


## v1.0.9 - 2026-07-09

- 类型：Explore 应用库导航热修。
- 目标：修复点击 Explore 左侧“应用库”后页面先进入应用库又自动跳回默认已安装应用，导致视觉闪跳的问题。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.8` / `7a91e682`
- 发布引用：`v1.0.9` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.9`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.7`
- 镜像重建：仅 Web
- 数据库变更：否
- 重启服务：`web`、`nginx`

### 变更内容

- 移除 `/explore/apps` 页面加载后的自动 `router.replace('/explore/installed/...')` 逻辑。
- 保留左侧具体已安装应用条目的跳转行为；用户点击“应用库”时现在会稳定停留在应用库列表页。
- 继续保留 Explore 顶部导航、已安装应用侧栏和应用详情聊天页现有行为。

### 验证记录

- `git diff --check`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- 内置浏览器验证：点击 Explore 左侧“应用库”后停留在 `/explore/apps`，不再闪跳回 `/explore/installed/...`；点击具体应用仍可进入对应 installed app。

### 回退方式

- 将 `docker/.env` 的 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.8`，`MMB_RELEASE_VERSION` 恢复为 `v1.0.8`，`COMMIT_SHA` 恢复为 `7a91e682d2b09ca4503c9bf303f2343ff0df23c9`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。
- 回退到 v1.0.8 会恢复“应用库”入口自动跳回默认已安装应用的行为。


## v1.0.8 - 2026-07-09

- 类型：历史会话共性修复与账号隔离 Web 热修。
- 目标：修复 v1.0.7 中“当前会话不在已加载列表就清空”的误伤，避免普通账号看不到自己的历史会话；同时让 Explore installed app 的本地会话选择按登录账号隔离。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.7` / `bafaca8e`
- 发布引用：`v1.0.8` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.8`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.7`
- 镜像重建：仅 Web
- 数据库变更：否
- 重启服务：`web`、`nginx`

### 变更内容

- 移除 v1.0.7 中基于“当前会话不在普通/置顶第一页列表”的自动清空逻辑，避免分页、列表慢加载或账号切换时误清空。
- 保留消息接口明确返回 `404` / `Conversation Not Exists` 时的容错清空，防止旧无权限会话反复弹错。
- Explore installed app 的 `conversationIdInfo` 改为按 `appSourceType + appId + account:id` 存储，避免同浏览器多账号共用 `DEFAULT` conversation id。
- 对旧 `conversationIdInfo[appId]` 做安全迁移：只有旧 id 确实出现在当前账号已加载会话列表中，才迁移到新的账号隔离 key。

### 验证记录

- `git diff --check`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- 内置浏览器验证：mmbadmin 和普通账号历史会话可见并可点击加载；切换账号不再请求上一个账号的旧 conversation id；`/apps` 仍正常显示。

### 回退方式

- 将 `docker/.env` 的 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.7`，`MMB_RELEASE_VERSION` 恢复为 `v1.0.7`，`COMMIT_SHA` 恢复为 `bafaca8ef029dbef2461d9c4285120fca67ef37e`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。
- 回退到 v1.0.7 会恢复历史会话误清空风险，只建议临时应急。


## v1.0.7 - 2026-07-09

- 类型：旧会话容错与 Workflows 损坏数据恢复。
- 目标：修复 Explore 残留旧 `conversation_id` 反复弹 `Conversation Not Exists`，并修复 `/apps` 因两条 draft workflow `graph` TOAST 损坏导致 500。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.6` / `8b928f0a`
- 发布引用：`v1.0.7` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.7`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.7`
- 镜像重建：API 与 Web
- 数据库变更：是，仅修复两条 draft workflow 的 `graph` 字段
- 数据备份：`/opt/mmb-dify/current/ops-backups/workflow-graph-repair-v107-20260709092546`
- 重启服务：`api`、`api_websocket`、`worker`、`worker_beat`、`web`、`nginx`

### 变更内容

- Explore 聊天页在当前会话消息接口返回 404 / `Conversation Not Exists` 时，自动清空当前选中会话并回到新对话欢迎态。
- 会话列表加载完成后，如果本地保存的当前会话 id 不在可见会话列表或置顶列表中，自动降级为新对话状态，避免继续请求无权限或不存在的旧会话。
- `/console/api/apps` 判断 draft trigger 时改为逐条读取 workflow；单条 draft workflow 的 `graph` 损坏只记录日志并跳过该条，不再拖垮整个应用列表接口。
- 已修复 `发布海报助手` 和 `MMB业务助手-v4.1` 两条损坏 draft workflow 的 `graph`，分别回填同 app 可读 workflow graph。

### 验证记录

- `python3 -m py_compile api/controllers/console/app/app.py`
- `pytest`：`api/tests/unit_tests/controllers/console/app/test_app_response_models.py`
- `pnpm test web/app/components/base/chat/chat-with-history/__tests__/hooks.spec.tsx`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build api web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- 浏览器验证：`/apps` 正常显示应用列表；Explore 默认应用可进入，有效历史会话正常加载，旧会话 id 不再连续弹错。

### 回退方式

- 将 `docker/.env` 的 `DIFY_API_IMAGE` 恢复为 `mmbai/dify-api:dify-mmb-v1.0.5`，`DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.6`，`MMB_RELEASE_VERSION` 恢复为 `v1.0.6`，`COMMIT_SHA` 恢复为 `8b928f0ad2c730edeb04a83f5a61964d5f7b3227`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。
- 数据回滚可使用上述备份目录恢复两条 workflow；若原始 `graph` 已不可读，保留修复后的可读 graph 更安全。


## v1.0.6 - 2026-07-08

- 类型：企业后台账号管理入口热修。
- 目标：把 v1.0.5 已上线的直接创建账号和重置密码能力接入当前 `/admin` 实际渲染的 RBAC 成员目录页面。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.5` / `a169e9f8`
- 发布引用：`v1.0.6` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.6`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.5`
- 镜像重建：仅 Web
- 数据库变更：否
- 重启服务：`web`、`nginx`

### 变更内容

- 在真实线上 `/admin` 使用的 RBAC 成员目录顶部新增“新增账号”按钮，打开直接创建账号弹窗。
- 成员行操作区新增“重置密码”按钮，按 owner/admin/self/目标角色规则显示。
- 新增账号和重置密码成功后刷新成员列表与审计日志。
- 审计日志列表纳入 `member.direct_create` 与 `member.password.reset` 操作。

### 验证记录

- `git diff --check`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- 浏览器验证：`/admin` 成员目录显示“新增账号”，可打开新增账号弹窗；普通成员行显示“重置密码”按钮并可打开重置密码弹窗。

### 回退方式

- 将 `docker/.env` 的 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.5`，`MMB_RELEASE_VERSION` 恢复为 `v1.0.5`，`COMMIT_SHA` 恢复为 `a169e9f83f449d0aa6df3117ab6f796f51f88cec`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。
- 本次无数据库迁移，API 镜像无需回退。


## v1.0.5 - 2026-07-08

- 类型：企业后台成员账号管理增强。
- 目标：管理员可在企业管理后台直接创建员工账号并设置初始密码，也可为符合权限规则的成员重置密码。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.4` / `3d393799`
- 发布引用：`v1.0.5` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.5`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.5`
- 镜像重建：API 与 Web
- 数据库变更：否
- 重启服务：`api`、`api_websocket`、`worker`、`worker_beat`、`web`、`nginx`

### 变更内容

- 新增 `POST /console/api/workspaces/current/members`，支持 admin/owner 在当前工作区直接创建账号、设置初始密码并指定非 owner 角色。
- 新增账号时校验邮箱唯一、密码强度、成员上限、dataset operator 开关和角色越权；已有邮箱一律拒绝，不自动加入工作区也不重设密码。
- 企业管理后台“账号管理”中的“新增账号”改为直接创建账号弹窗，不再走邀请邮件弹窗。
- 成员行新增“重置密码”操作，复用已有成员密码重置接口；owner 可重置非 owner，admin 只能重置普通成员、编辑者和知识库管理员。
- 成员直建和密码重置写入 `operation_logs` 审计记录，记录目标账号和角色，不记录明文密码。

### 验证记录

- `python3 -m py_compile api/controllers/console/workspace/members.py api/tests/unit_tests/controllers/console/workspace/test_members.py`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env run --rm --no-deps --entrypoint python api -m pytest -o addopts='' tests/unit_tests/controllers/console/workspace/test_members.py tests/unit_tests/services/test_account_service.py`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build api web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env ps api api_websocket worker worker_beat web nginx`
- `curl -kI https://ai.meinmalzebier.shop/admin`
- 企业后台浏览器验证：新增普通账号可直接登录；重置普通成员密码后新密码可登录。

### 回退方式

- 将 `docker/.env` 的 `DIFY_API_IMAGE` 恢复为 `mmbai/dify-api:dify-mmb-v1.0.3`，`DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.4`，`MMB_RELEASE_VERSION` 恢复为 `v1.0.4`，`COMMIT_SHA` 恢复为 `3d39379931f51e1ae8be00af74cdd9b603f55b9c`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。
- 本次无数据库迁移，回退不需要 Alembic downgrade。

## v1.0.4 - 2026-07-08

- 类型：普通员工设置页权限体验热修。
- 目标：普通员工点击右上角设置时只进入个人语言设置，避免前端打开成员管理并触发后端 403。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.3` / `50c5a600`
- 发布引用：`v1.0.4` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.4`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.3`
- 镜像重建：仅 Web
- 数据库变更：否
- 重启服务：`web`、`nginx`

### 变更内容

- 账户设置弹窗按当前工作区角色过滤可见 tab：owner/admin 保持成员、模型供应商、数据源、API 扩展等管理项；普通员工和 dataset operator 只显示通用组里的语言设置。
- 普通员工从右上角账户菜单点击“设置”时默认打开语言设置；管理员/owner 仍默认打开成员管理。
- 对外部传入的无权限设置 tab 做前端兜底，普通员工即使收到 `MEMBERS`、`PROVIDER`、`DATA_SOURCE` 等 tab，也自动降级渲染 `LANGUAGE`，不再挂载成员页或发起成员列表请求。
- 后端权限保持不变，成员管理、邀请、重置密码接口继续由 `@is_admin_or_owner_required` 保护；新注册默认角色仍为 `normal`。

### 验证记录

- `pnpm -C web test app/components/header/account-setting/__tests__/index.spec.tsx app/components/header/account-dropdown/__tests__/index.spec.tsx`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env ps web nginx`

### 回退方式

- 将 `docker/.env` 的 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:dify-mmb-v1.0.3`，`MMB_RELEASE_VERSION` 恢复为 `v1.0.3`，`COMMIT_SHA` 恢复为 `50c5a600`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。

## v1.0.3 - 2026-07-08

- 类型：员工账号注册管理功能发布。
- 目标：开放员工邮箱+密码自助注册，注册后自动进入 MMBAI 主团队，并把登录/应用库入口默认引导到 MMB智囊应用。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.2` / `3fa78feb`
- 发布引用：`v1.0.3` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.3`
- API 镜像：`mmbai/dify-api:dify-mmb-v1.0.3`
- 镜像重建：API 与 Web
- 数据库变更：是，新增 `accounts.token_version`，用于密码重置后使旧 access token 失效。
- 重启服务：`api`、`api_websocket`、`worker`、`worker_beat`、`web`、`nginx`

### 变更内容

- 新增直接密码注册模式：`ALLOW_REGISTER=true`、`REGISTER_MODE=direct_password` 时，注册页显示邮箱、密码、确认密码表单，不再要求邮件验证码。
- 新注册账号自动加入 MMBAI 主团队 `60630e96-a41c-481d-92e2-c6f83c9e4749`，默认角色为 `normal`。
- 登录成功后默认进入 `/explore/apps`，若存在默认已安装应用 `ba2250f2-ad47-4e47-95c7-3acfad955111` 则自动跳转到该应用，否则跳转到第一个可见已安装应用。
- 成员管理操作菜单新增“重置密码”，owner 可重置非 owner 成员密码，admin 只能重置非 owner/admin 成员密码。
- 密码重置会递增 `accounts.token_version` 并清理 refresh token；新 access token 带版本号，旧无版本 token 仅在账号版本仍为 0 时兼容。
- 补齐生产库当前 Alembic 链上已有但源码缺失的父迁移 `e5f6a7b8c9d0`、`f1a2b3c4d5e6`，新增迁移 `a1b2c3d4e5f7`。

### 验证记录

- `python3 -m py_compile api/controllers/console/auth/email_register.py api/controllers/console/workspace/members.py api/services/account_service.py api/extensions/ext_login.py api/services/feature_service.py api/models/account.py api/migrations/versions/2026_07_08_0100-a1b2c3d4e5f7_add_account_token_version.py`
- `python3 -m json.tool web/i18n/en-US/common.json`
- `python3 -m json.tool web/i18n/zh-Hans/common.json`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env run --rm --no-deps --entrypoint python api -m pytest -o addopts='' tests/unit_tests/services/test_account_service.py tests/unit_tests/controllers/console/workspace/test_members.py`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build api web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env run --rm --no-deps -e MODE=migration api`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env ps`
- `curl -kI https://ai.meinmalzebier.shop/signup`
- `curl -kI https://ai.meinmalzebier.shop/explore/apps`
- `curl -k https://ai.meinmalzebier.shop/console/api/system-features`

### 回退方式

- 恢复本次发布前备份的 `docker/.env.bak.before-register-v103-*`，将 API/Web 镜像恢复到 v1.0.2 记录的镜像。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d api api_websocket worker worker_beat web`，随后 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env restart nginx`。
- 如已执行数据库迁移且需要完全回滚，先确认无新注册/密码重置依赖后再执行 Alembic downgrade 移除 `accounts.token_version`。

## v1.0.2 - 2026-07-07

- 类型：生产品牌视觉正式修复。
- 目标：把 MMB 小熊 loading 与左上角小熊 logo 固化进正式 Web 镜像，修复 SSR 侧 API 前缀回落到 localhost 的问题。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 发布引用：`v1.0.2` tag target
- Web 镜像：`mmbai/dify-web:dify-mmb-v1.0.2`
- API 镜像：`mmbai/dify-api:channel-main-agent-3768115ce2`
- 镜像重建：仅 Web
- 数据库变更：否
- 重启服务：`web`

### 变更内容

- 正式重建 Web 镜像，让 `.next` 与 `vinext/dist` 编译产物包含 `mmb-bear-bottle-transparent.png` 小熊 loading。
- 保持浏览器端 API 前缀为同源相对路径 `/console/api`、`/api`，避免前端请求暴露 Docker 内网地址。
- Web SSR 在服务端遇到相对 API 前缀时自动转为 Docker 内网 `http://api:5001/...`，避免继续请求 `http://localhost/console/api/system-features`。
- 保留 nginx 品牌 patch 与 favicon 路由，但不再依赖热修 minified 编译产物修复 loading。

### 验证记录

- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env build web`
- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`
- `grep -R mmb-bear-bottle-transparent /app/targets/next/web/.next /app/targets/vinext/dist`
- `curl -k https://ai.meinmalzebier.shop/apps`
- `curl -k https://ai.meinmalzebier.shop/signin`
- 浏览器验证 `/signin`、`/apps`、工作流页左上角 logo。

### 回退方式

- 将 `docker/.env` 的 `DIFY_WEB_IMAGE` 恢复为 `mmbai/dify-web:channel-main-agent-613f97d0f1`。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`。

## v1.0.1 - 2026-07-06

- 类型：生产恢复热修。
- 目标：恢复 `v1.0.0` 回滚后缺失的 Web 运行配置、favicon 路径和 MMB 小熊载入静态资源。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 来源版本：`v1.0.0` / `034e8696`
- 发布引用：`v1.0.1` tag
- Web 镜像：`mmbai/dify-web:channel-main-agent-613f97d0f1`
- API 镜像：`mmbai/dify-api:channel-main-agent-3768115ce2`
- 镜像重建：否
- 数据库变更：否
- 重启服务：`web`、`nginx`

### 变更内容

- 修复 Web 运行态 URL 配置：浏览器 API 前缀恢复为同源相对路径 `/console/api`、`/api`，`NEXT_PUBLIC_SOCKET_URL` 保持 `wss://ai.meinmalzebier.shop`。
- 避免把 Docker 内网 `http://api:5001` 暴露给前端，同时避免绝对 API URL 破坏未登录/401 跳转。
- 为 `/mmb-favicon-refresh-20260705.ico` 增加 nginx 精确路由，避免缺失 favicon 回落成大 HTML 页面。
- 恢复 `mmb-bear-bottle-transparent.png` 小熊载入静态资源，并在源码中恢复 `Loading` 组件为小熊动效。
- 当前运行容器只补静态资源，不再热修已编译 `_next` chunk；小熊 loading 组件将在下一次正式 Web 镜像构建后固化生效。
- 原计划“企业权限控制台成员目录添加成员”顺延到 `v1.0.2`。

### 验证记录

- `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env ps`
- `curl -k https://ai.meinmalzebier.shop/console/api/system-features`
- `curl -k https://ai.meinmalzebier.shop/mmb-favicon-refresh-20260705.ico?v=20260705-fix2`
- `curl -k https://ai.meinmalzebier.shop/custom-assets/mmb-loading/mmb-bear-bottle-transparent.png`
- `docker logs --since 5m mmb-dify-v010-web-1`

### 回退方式

- 恢复本次热修前的 `docker/.env` 和 `docker/nginx/conf.d/default.conf.template` 备份。
- 执行 `docker compose -p mmb-dify-v010 -f docker/docker-compose.yaml --env-file docker/.env up -d web`，再重启 `nginx`。

## v1.0.0 - 2026-07-06

- 类型：补登记生产基线版本。
- 含义：150 生产机上一次真实稳定 Dify-MMB 部署状态，作为后续正式版本线起点。
- 生产路径：`/opt/mmb-dify/current`
- Compose project：`mmb-dify-v010`
- 基线源码提交：`597823fb`
- 之前部署记录：`v0.1.0` / `c7c2ec2312e3c15f69ca1c15885c250b969566ac`
- 生产 Web 镜像：`mmbai/dify-web:channel-main-agent-613f97d0f1`
- 生产 Web 镜像 ID：`sha256:7b7beb1a19b3dfe9c4052b40614bd60b2eb2b7250fa19b37158a6f698ee75bd0`
- 生产 API 镜像：`mmbai/dify-api:channel-main-agent-3768115ce2`
- 生产 API 镜像 ID：`sha256:fa9d0ab0b0b032c895eaa6ed80aebb5ea8c0bbda7fc55b3f57f44cff1a4868a5`
- 说明：该版本只补齐版本治理记录，不从 T1000 重新构建，不覆盖 150 当前生产运行状态。

### 验证记录

- `docker compose -p mmb-dify-v010 ps`
- `git status --short`
- `docker image inspect mmbai/dify-web:channel-main-agent-613f97d0f1`
- `docker image inspect mmbai/dify-api:channel-main-agent-3768115ce2`

### 回退方式

- 使用本节记录的 Web/API 镜像 tag 或 image ID 恢复 `docker/.env` 中的 `DIFY_WEB_IMAGE`、`DIFY_API_IMAGE`。
- 执行 `docker compose -p mmb-dify-v010 up -d api api_websocket worker worker_beat web`。
